#pragma once
// Internal include-path shim. Resolves the quoted include "modes/mode_api.h"
// to the public mirror below when only the SDK's include/ directory is on the
// path. Never include this file directly.

#include <operator_sdk/abi/mode_api.h>
